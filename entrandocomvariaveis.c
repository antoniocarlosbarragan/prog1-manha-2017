#include <stdio.h>

main ()
{
int x;
int y;
int multiplicacao;

printf("\nEntre com o valor de X:");
scanf("%d", &x);

printf("\nEntre com o valor de Y:");
scanf("%d", &y);
  
multiplicacao= x * y;

printf("\nResultado da multiplicação = %d\n", multiplicacao);

return 0;
}
